# Core Dependencies
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0
rich>=13.7.0
psutil>=5.9.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0

# Voice Subsystem (primary — Python 3.14 safe)
edge-tts>=6.1.0
# pyttsx3>=2.90  -- Windows SAPI5 TTS fallback (optional).
#                   Python 3.14 compatibility unconfirmed; installed via: pip install ".[voice-sapi]"
#                   ZEN lazy-imports pyttsx3 and falls back to edge-tts if unavailable.

# Web Intelligence
duckduckgo-search>=6.0.0

# Testing & Dev
pytest>=8.0.0
pytest-asyncio>=0.23.0
